<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit User</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body class="bg-gray-50">
    <%- include('../../partials/header'); %>
    <div class="max-w-2xl mx-auto my-10">
        <div class="mb-6">
            <h1 class="text-lg md:text-2xl font-bold text-[#FF214F]">Edit User</h1>
        </div>

        <div class="bg-white rounded-xl shadow p-6">
            <form action="/users/<%= user.id %>" method="POST" class="space-y-4">
                <input type="hidden" value="PUT">

                <div>
                    <label class="text-gray-800 text-sm font-medium block mb-1.5">Username *</label>
                    <input name="username" value="<%= user.username %>"
                        class="w-full h-11 px-4 rounded-lg border border-gray-300 focus:outline-none focus:ring-2 focus:ring-[#FF214F]"
                        type="text" required />
                </div>

                <div>
                    <label class="text-gray-800 text-sm font-medium block mb-1.5">Email Address *</label>
                    <input name="email" value="<%= user.email %>"
                        class="w-full h-11 px-4 rounded-lg border border-gray-300 focus:outline-none focus:ring-2 focus:ring-[#FF214F]"
                        type="email" required />
                </div>

                <div>
                    <label class="text-gray-800 text-sm font-medium block mb-1.5">Role *</label>
                    <select name="role"
                        class="w-full h-11 px-4 rounded-lg border border-gray-300 focus:outline-none focus:ring-2 focus:ring-[#FF214F] cursor-pointer"
                        required>
                        <option value="USER" <%= user.role === 'USER' ? 'selected' : '' %>>User</option>
                        <option value="ADMIN" <%= user.role === 'ADMIN' ? 'selected' : '' %>>Admin</option>
                    </select>
                </div>

                <div>
                    <label class="text-gray-800 text-sm font-medium block mb-1.5">Image URL</label>
                    <input name="image" value="<%= user.image || '' %>"
                        class="w-full h-11 px-4 rounded-lg border border-gray-300 focus:outline-none focus:ring-2 focus:ring-[#FF214F]"
                        type="url" placeholder="https://..." />
                </div>

                <% if (user.image) { %>
                    <div class="bg-gray-50 p-4 rounded-lg">
                        <p class="text-sm text-gray-700 mb-2 font-medium">Current Image:</p>
                        <img src="<%= user.image %>" alt="<%= user.username %>" class="w-24 h-24 rounded-full object-cover">
                    </div>
                <% } %>

                <div class="bg-blue-50 p-4 rounded-lg">
                    <h3 class="font-medium text-gray-800 mb-3">User Statistics:</h3>
                    <div class="grid grid-cols-2 gap-3 text-sm text-gray-600">
                        <div>
                            <span class="font-medium">Status:</span>
                            <span class="ml-1 px-2 py-0.5 rounded text-xs <%= user.verified ? 'bg-blue-100 text-blue-800' : 'bg-yellow-100 text-yellow-800' %>">
                                <%= user.verified ? 'Verified' : 'Pending' %>
                            </span>
                        </div>
                        <div>
                            <span class="font-medium">Favorite Bands:</span>
                            <span class="text-gray-800 font-semibold">
                                <%= user.favoriteBands ? user.favoriteBands.length : 0 %>
                            </span>
                        </div>
                        <div>
                            <span class="font-medium">Wishlist:</span>
                            <span class="text-gray-800 font-semibold">
                                <%= user.wishList ? user.wishList.length : 0 %>
                            </span>
                        </div>
                        <div>
                            <span class="font-medium">Tickets:</span>
                            <span class="text-gray-800 font-semibold">
                                <%= user.tickets ? user.tickets.length : 0 %>
                            </span>
                        </div>
                    </div>
                </div>

                <div class="flex gap-3 pt-4">
                    <a href="/users"
                        class="px-4 py-2 border border-gray-300 rounded-lg text-gray-700 hover:text-[#FF214F] hover:border-[#FF214F] hover:bg-gray-50 transition-colors">
                        Cancel
                    </a>
                    <button type="submit"
                        class="cursor-pointer px-4 py-2 bg-[#FF214F] hover:bg-[#f83159ff] text-white rounded-lg transition-colors">
                        Update User
                    </button>
                </div>
            </form>
        </div>
    </div>
</body>
</html>