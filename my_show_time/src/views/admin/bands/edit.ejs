<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Band</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body>

<%- include('../../partials/header'); %>
<div class="max-w-2xl mx-auto my-10">
    <div class="mb-6">
        <h1 class="text-lg md:text-2xl font-bold text-[#FF214F]">Edit Band</h1>
    </div>

    <div class="bg-white rounded-xl shadow p-6">
        <form action="/groups/<%= band.id %>" method="POST" class="space-y-4">
            <input type="hidden" value="PUT">

            <div>
                <label class="text-gray-800 text-sm font-medium block mb-1.5">Band Name *</label>
                <input name="name" value="<%= band.name %>"
                    class="w-full h-11 px-4 rounded-lg border border-gray-300 focus:outline-none focus:ring-2 focus:ring-[#FF214F]"
                    type="text" required />
            </div>

            <div>
                <label class="text-gray-800 text-sm font-medium block mb-1.5">Biography *</label>
                <textarea name="bio" rows="4"
                    class="w-full px-4 py-2 rounded-lg border border-gray-300 focus:outline-none focus:ring-2 focus:ring-[#FF214F]"
                    required><%= band.bio %></textarea>
            </div>

            <div>
                <label class="text-gray-800 text-sm font-medium block mb-1.5">Genre *</label>
                <select name="genre"
                    class="w-full h-11 px-4 rounded-lg border border-gray-300 focus:outline-none focus:ring-2 focus:ring-[#FF214F] cursor-pointer"
                    required>
                    <option value="ROCK" <%=band.genre==='ROCK' ? 'selected' : '' %>>Rock</option>
                    <option value="POP" <%=band.genre==='POP' ? 'selected' : '' %>>Pop</option>
                    <option value="JAZZ" <%=band.genre==='JAZZ' ? 'selected' : '' %>>Jazz</option>
                    <option value="ELECTRONIC" <%=band.genre==='ELECTRONIC' ? 'selected' : '' %>>Electronic</option>
                    <option value="HIP_HOP" <%=band.genre==='HIP_HOP' ? 'selected' : '' %>>Hip Hop</option>
                    <option value="CLASSICAL" <%=band.genre==='CLASSICAL' ? 'selected' : '' %>>Classical</option>
                    <option value="METAL" <%=band.genre==='METAL' ? 'selected' : '' %>>Metal</option>
                    <option value="INDIE" <%=band.genre==='INDIE' ? 'selected' : '' %>>Indie</option>
                    <option value="OTHER" <%=band.genre==='OTHER' ? 'selected' : '' %>>Other</option>
                </select>
            </div>

            <div>
                <label class="text-gray-800 text-sm font-medium block mb-1.5">Image URL</label>
                <input name="image" value="<%= band.image || '' %>"
                    class="w-full h-11 px-4 rounded-lg border border-gray-300 focus:outline-none focus:ring-2 focus:ring-[#FF214F]"
                    type="url" />
            </div>

            <% if (band.image) { %>
                <div class="bg-gray-50 p-4 rounded-lg">
                    <p class="text-sm text-gray-700 mb-2 font-medium">Current Image:</p>
                    <img src="<%= band.image %>" alt="<%= band.name %>" class="w-32 h-32 rounded-lg object-cover">
                </div>
                <% } %>

                    <div class="bg-blue-50 p-4 rounded-lg">
                        <h3 class="font-medium text-gray-800 mb-2">Statistics:</h3>
                        <div class="grid grid-cols-2 gap-3 text-sm text-gray-600">
                            <div>
                                <span class="font-medium">Concerts:</span>
                                <%= band.concertIds ? band.concertIds.length : 0 %>
                            </div>
                            <div>
                                <span class="font-medium">Fans:</span>
                                <%= band.fanIds ? band.fanIds.length : 0 %>
                            </div>
                        </div>
                    </div>

                    <div class="flex gap-3 pt-4">
                        <a href="/groups"
                            class="px-4 py-2 border border-gray-300 rounded-lg text-gray-700 hover:text-[#FF214F] hover:border-[#FF214F] hover:bg-gray-50 transition-colors">
                            Cancel
                        </a>
                        <button type="submit"
                            class="cursor-pointer px-4 py-2 bg-[#FF214F] hover:bg-[#f83159ff] text-white rounded-lg transition-colors">
                            Update Band
                        </button>
                    </div>

        </form>
    </div>
</div>
    
</body>
</html>