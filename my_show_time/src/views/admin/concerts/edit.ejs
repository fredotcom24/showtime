<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Concert</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body>
    
<%- include('../../partials/header'); %>
<div class="max-w-3xl mx-auto my-10 ">
    <div class="mb-6">
        <h1 class="text-lg md:text-2xl font-bold text-[#FF214F]">Edit Concert</h1>
    </div>

    <div class="bg-white rounded-xl shadow p-6">
        <form action="/concerts/<%= concert.id %>" method="POST" class="space-y-4">
            <input type="hidden" value="PUT">

            <div>
                <label class="text-gray-800 text-sm font-medium block mb-1.5">Concert Name *</label>
                <input name="title" value="<%= concert.name %>"
                    class="w-full h-11 px-4 rounded-lg border border-gray-300 focus:outline-none focus:ring-2 focus:ring-[#FF214F]"
                    type="text" required />
            </div>

            <div>
                <label class="text-gray-800 text-sm font-medium block mb-1.5">Description</label>
                <textarea name="description" rows="3"
                    class="w-full px-4 py-2 rounded-lg border border-gray-300 focus:outline-none focus:ring-2 focus:ring-[#FF214F]"><%= concert.description || '' %></textarea>
            </div>

            <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                <div>
                    <label class="text-gray-800 text-sm font-medium block mb-1.5">Genre *</label>
                    <select name="genre"
                        class="w-full h-11 px-4 rounded-lg border border-gray-300 focus:outline-none focus:ring-2 focus:ring-[#FF214F] cursor-pointer"
                        required>
                        <option value="ROCK" <%=concert.genre==='ROCK' ? 'selected' : '' %>>Rock</option>
                        <option value="POP" <%=concert.genre==='POP' ? 'selected' : '' %>>Pop</option>
                        <option value="JAZZ" <%=concert.genre==='JAZZ' ? 'selected' : '' %>>Jazz</option>
                        <option value="ELECTRONIC" <%=concert.genre==='ELECTRONIC' ? 'selected' : '' %>>Electronic
                        </option>
                        <option value="HIP_HOP" <%=concert.genre==='HIP_HOP' ? 'selected' : '' %>>Hip Hop</option>
                        <option value="CLASSICAL" <%=concert.genre==='CLASSICAL' ? 'selected' : '' %>>Classical</option>
                        <option value="METAL" <%=concert.genre==='METAL' ? 'selected' : '' %>>Metal</option>
                        <option value="INDIE" <%=concert.genre==='INDIE' ? 'selected' : '' %>>Indie</option>
                        <option value="OTHER" <%=concert.genre==='OTHER' ? 'selected' : '' %>>Other</option>
                    </select>
                </div>

                <div>
                    <label class="text-gray-800 text-sm font-medium block mb-1.5">Date & Time *</label>
                    <input name="date" value="<%= new Date(concert.date).toISOString().slice(0, 16) %>"
                        class="w-full h-11 px-4 rounded-lg border border-gray-300 focus:outline-none focus:ring-2 focus:ring-[#FF214F] cursor-pointer"
                        type="datetime-local" required />
                </div>
            </div>

            <div>
                <label class="text-gray-800 text-sm font-medium block mb-1.5">Location *</label>
                <input name="location" value="<%= concert.location %>"
                    class="w-full h-11 px-4 rounded-lg border border-gray-300 focus:outline-none focus:ring-2 focus:ring-[#FF214F]"
                    type="text" required />
            </div>

            <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                <div>
                    <label class="text-gray-800 text-sm font-medium block mb-1.5">Price ($) *</label>
                    <input name="price" value="<%= concert.price %>"
                        class="w-full h-11 px-4 rounded-lg border border-gray-300 focus:outline-none focus:ring-2 focus:ring-[#FF214F]"
                        type="number" step="0.01" min="0" required />
                </div>

                <div>
                    <label class="text-gray-800 text-sm font-medium block mb-1.5">Image URL</label>
                    <input name="imageUrl" value="<%= concert.image || '' %>"
                        class="w-full h-11 px-4 rounded-lg border border-gray-300 focus:outline-none focus:ring-2 focus:ring-[#FF214F]"
                        type="url" />
                </div>
            </div>

            <div class="bg-gray-50 p-4 rounded-lg">
                <h3 class="font-medium text-gray-800 mb-2">Current bands:</h3>
                <% if (concert.groups && concert.groups.length > 0) { %>
                    <div class="flex flex-wrap gap-2">
                        <% concert.groups.forEach(group=> { %>
                            <span class="px-3 py-1 bg-blue-100 text-blue-800 rounded-full text-sm">
                                <%= group.name %>
                            </span>
                        <% }); %>
                    </div>
                    <% } else { %>
                        <p class="text-sm text-gray-500">No bands assigned</p>
                    <% } %>
            </div>

            <div class="flex gap-3 pt-4">
                <a href="/concerts/admin"
                    class="px-4 py-2 border border-gray-300 rounded-lg text-gray-700 hover:text-[#FF214F] hover:border-[#FF214F] hover:bg-gray-50 transition-colors">
                    Cancel
                </a>
                <button type="submit"
                    class="cursor-pointer px-4 py-2 bg-[#FF214F] hover:bg-[#f83159ff] text-white rounded-lg transition-colors">
                    Update Concert
                </button>
            </div>

        </form>
    </div>
</div>
</body>
</html>