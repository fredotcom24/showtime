<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>All Bands</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>

<body>
    <%- include('../partials/header'); %>

<main class="flex-1 p-4 sm:p-6 overflow-y-auto max-w-7xl">
    <div class="flex items-center justify-between mb-6">
        <div>
            <h1 class="text-lg md:text-2xl font-bold">All Bands</h1>
        </div>

    </div>

    <!-- Desktop Table -->
    <section class=" bg-white shadow rounded-2xl p-6 w-7xl">
        <table class="w-7xl text-sm text-left border-collapse overflow-auto">
            <thead class="bg-[#FF214F] text-white rounded-t-xl">
                <tr>
                    <th class="px-6 py-3 font-semibold">Image</th>
                    <th class="px-6 py-3 font-semibold">Name</th>
                    <th class="px-6 py-3 font-semibold">Genre</th>
                    <th class="px-6 py-3 font-semibold">Concerts</th>
                    <th class="px-6 py-3 font-semibold">Fans</th>
                    <th class="px-6 py-3 font-semibold">Created At</th>
                    <th class="px-6 py-3 font-semibold">Actions</th>
                </tr>
            </thead>

            <tbody>
                <% if (bands && bands.length> 0) { %>
                    <% bands.forEach(band=> { %>
                        <tr class="border-b border-gray-200 hover:bg-gray-50 transition">
                            <td class="px-6 py-3">
                                <% if (band.image) { %>
                                    <img src="<%= band.image %>" alt="<%= band.name %>"
                                        class="w-12 h-12 rounded-full object-cover">
                                <% } else { %>
                                <div class="w-12 h-12 rounded-full bg-gray-200 flex items-center justify-center">
                                    <span class="text-gray-500 text-xs font-semibold">
                                        <%= band.name.charAt(0) %>
                                    </span>
                                </div>
                                <% } %>
                            </td>
                            <td class="px-6 py-3 text-gray-800 font-medium">
                                <%= band.name %>
                            </td>
                            <td class="px-6 py-3">
                                <span class="px-3 py-1 rounded-full text-xs font-semibold bg-purple-100 text-purple-800">
                                    <%= band.genre %>
                                </span>
                            </td>
                            <td class="px-6 py-3 text-gray-600">
                                <%= band.concertIds ? band.concertIds.length : 0 %> concerts
                            </td>
                            <td class="px-6 py-3 text-gray-600">
                                <%= band.fanIds ? band.fanIds.length : 0 %> fans
                            </td>
                            <td class="px-6 py-3 text-gray-600">
                                <%= new Date(band.createdAt).toLocaleDateString() %>
                            </td>
                            <td class="px-6 py-3">
                                <div class="flex gap-2 font-bold">
                                    <!-- <a href="/groups/<%= band.id %>/edit"
                                        class="bg-black cursor-pointer text-white px-3 py-1 rounded-full hover:opacity-90">
                                        Details
                                    </a> -->
                                    <form action="/users/<%= user.id %>/favorites/groups"  method="post">
                                    <input type="text" class="sr-only" value="<%= band.id %>" name="groupId">
                                    <button type="submit"
                                        class="cursor-pointer bg-black hover:bg-red-700 cursor-pointer text-white px-3 py-1 rounded-full hover:opacity-90">
                                        + Favorite
                                    </button>
                                    </form>
                                </div>
                            </td>
                        </tr>
                        <% }); %>
                            <% } else { %>
                                <tr>
                                    <td colspan="7" class="text-center py-6 text-gray-500">
                                        No bands found
                                    </td>
                                </tr>
                                <% } %>
            </tbody>
        </table>
    </section>


</main>
</body>


<script>
    function confirmDelete(type, id, name) {
        if (confirm(`Are you sure you want to delete "${name}" ?\n\nThis action is irreversible.`)) {
            const button = event.target;
            const originalText = button.innerHTML;
            button.disabled = true;
            button.innerHTML = 'Deletion...';
            button.classList.add('opacity-50', 'cursor-not-allowed');

            fetch(`http://localhost:3000/groups/${id}`, {
                method: 'DELETE',
                headers: {
                    'Content-Type': 'application/json',
                }
            })
            .then(response => {
                console.log(response.status);
                if (!response.ok) {
                    throw new Error(`HTTP error! status: ${response.status}`);
                }
                return response.json();
            })
            .then(data => {
                console.log(data);
                if (data.success) {
                    alert('Deletion successful!');
                    window.location.reload();
                } else {
                    throw new Error('Deletion failed');
                }
            })
            .catch(error => {
                console.error('Error', error);
                alert('Error while deleting. Please try again.');
                
                button.disabled = false;
                button.innerHTML = originalText;
                button.classList.remove('opacity-50', 'cursor-not-allowed');
            });
        }
    }
</script>