
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Ticket - <%= ticket.concert.name %></title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
  </head>
  <body class="bg-gray-100 flex items-center justify-center min-h-screen p-4">
    <div class="w-full max-w-sm bg-white border border-gray-200 rounded-lg shadow-lg">
      
      <!-- Image of the concert -->
      <img
        class="w-full h-48 object-cover rounded-t-lg"
        src="<%= ticket.concert.image %>"
        alt="<%= ticket.concert.name %>"
      />

      <div class="p-6">
        <h5 class="mb-2 text-xl font-medium text-gray-500">SHOW TIME</h5>
        <h2 class="mb-4 text-2xl font-bold text-gray-900">
          <%= ticket.concert.name %>
        </h2>

        <!-- Informations of the concert -->
        <ul class="space-y-3 mb-6">
          <li class="flex items-center justify-between">
            <span class="text-gray-700">Date:</span>
            <span class="text-pink-800 text-xs font-semibold px-2.5 py-1 rounded">
              <%= new Date(ticket.concert.date).toLocaleDateString('en-US', {
                day: 'numeric',
                month: 'long',
                year: 'numeric',
                hour: '2-digit',
                minute: '2-digit'
                }) %>
            </span>
          </li>
          <li class="flex items-center justify-between">
            <span class="text-gray-700">Location:</span>
            <span class="text-pink-800 text-xs font-semibold px-2.5 py-1 rounded">
              <%= ticket.concert.location %>
            </span>
          </li>
          <li class="flex items-center justify-between">
            <span class="text-gray-700">Status:</span>
            <span class="text-green-800 text-xs font-semibold px-2.5 py-1 rounded">
              <%= ticket.status %>
            </span>
          </li>
        </ul>

        <!-- Price -->
        <div class="flex items-baseline justify-center mb-6">
          <span class="text-3xl font-semibold">$</span>
          <span class="text-5xl font-extrabold"><%= ticket.price.toFixed(2) %></span>
        </div>

        <!-- QR CODE (Generate image) -->
        <div class="mb-6 p-4 bg-gray-50 rounded-lg text-center">
          <p class="text-xs text-gray-500 mb-3 font-semibold">SCAN TO VALIDATE</p>
          <img 
            src="https://quickchart.io/qr?text=http://localhost:3000/concerts/<%= ticket.concert?.id %>&size=300" 
            alt="QR Code" 
            class="mx-auto w-48 h-48"
          />
          <p class="break-all text-xs text-gray-400 mt-2 font-mono">http://localhost:3000/concerts/<%= ticket.concert?.id %></p>
        </div>

        <!-- Download button -->
        <button
          type="button"
          class="w-full text-white bg-[#FF214F] hover:bg-[#e01d45] font-medium rounded-lg text-sm px-5 py-3"
          onclick="window.print()"
        >
          Download Ticket
        </button>
      </div>
    </div>
  </body>
</html>